# Backend DB config
# Preferred: full URL
#
# If you use this repo's docker-compose.yml, Postgres is on port 5433:
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/strong_fitness_studio_app

# Optional: individual params (used if DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=strong_fitness_studio_app
DB_USER=postgres
DB_PASSWORD=postgres

# Frontend dev server expects backend at:
# http://localhost:8000
